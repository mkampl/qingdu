# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Templates and Static Files
jinja2==3.1.2
aiofiles==23.2.1

# Chinese Text Processing
jieba==0.42.1
pypinyin==0.49.0

# HTTP Client
httpx==0.25.2

# Article extraction — server-side Readability-style cleaner so the user
# can paste any URL and get just the article body in the reader.
trafilatura==1.12.2

# Utilities
python-multipart==0.0.6
pydantic==2.5.0

# Database (for future features)
sqlalchemy==2.0.23

# environment vars
python-dotenv==1.0.0

# safer API
slowapi==0.1.9

# authentication
bcrypt==4.0.1
passlib==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.6

# anki export
genanki==0.13.0
gTTS==2.3.2

# Spaced repetition scheduler (Phase B)
fsrs==6.3.1

# Traditional ⇄ Simplified Chinese conversion (Phase E1)
opencc-python-reimplemented==0.1.7

# Server-side pronunciation assessment (Phase #95 follow-up):
# - faster-whisper bundles a CTranslate2-quantised Whisper ASR. We
#   load the "small" model on first request (~250 MB int8 after CT2
#   conversion). Replaces Web Speech for browsers without it (Firefox)
#   and lets us score tone-contour quality, not just text identity.
# - librosa is for librosa.pyin (F0 extraction) and audio I/O. ffmpeg
#   needs to be present at runtime — installed in the Dockerfile.
faster-whisper==1.0.3
librosa==0.10.2.post1

# EPUB / PDF import (Phase E2)
EbookLib==0.20
pypdf==6.12.1
beautifulsoup4==4.12.3
#pypinyin==0.49.0

# Retry logic
tenacity==8.2.3

# Caching
cachetools==5.3.2

# Optional: For better performance
# uvloop==0.19.0  # Unix only