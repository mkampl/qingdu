# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Templates and Static Files
jinja2==3.1.2
aiofiles==23.2.1

# Chinese Text Processing
jieba==0.42.1
pypinyin==0.49.0

# HTTP Client
httpx==0.25.2

# Article extraction — server-side Readability-style cleaner so the user
# can paste any URL and get just the article body in the reader.
trafilatura==1.12.2

# Utilities
python-multipart==0.0.6
pydantic==2.5.0

# Database (for future features)
sqlalchemy==2.0.23

# environment vars
python-dotenv==1.0.0

# safer API
slowapi==0.1.9

# authentication
bcrypt==4.0.1
passlib==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.6

# anki export
genanki==0.13.0
gTTS==2.3.2

# Spaced repetition scheduler (Phase B)
fsrs==6.3.1

# Traditional ⇄ Simplified Chinese conversion (Phase E1)
opencc-python-reimplemented==0.1.7


# EPUB / PDF import (Phase E2)
EbookLib==0.20
pypdf==6.12.1
beautifulsoup4==4.12.3
#pypinyin==0.49.0

# YouTube caption fetch for the watch-and-read prototype. Pure Python,
# hits the same public timedtext endpoint the YouTube player itself
# uses — no API key, no browser binary.
youtube-transcript-api==1.2.4

# Retry logic
tenacity==8.2.3

# Caching
cachetools==5.3.2

# Optional: For better performance
# uvloop==0.19.0  # Unix only